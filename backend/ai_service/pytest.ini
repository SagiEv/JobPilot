[pytest]
testpaths = tests
pythonpath = .
markers =
    security: prompt injection and safety tests
    schema: Pydantic schema validation tests
    integration: tests that exercise multiple components together
